#include "main.h"
/**
 * _abs - function to print absolute value of number
 * Return: 0
 * @int: type of argument
 */
int _abs(int)
{
	int n;

	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
